using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test : MonoBehaviour {

    private GameObject PlaneEmpty;
    public Transform PlaneEmptyPosition;
    public GameObject PrefabPosition;


    // Use this for initialization
    void Start () {
        /* PlaneEmpty = GameObject.Find("Airbus_Plane");
         PrefabPosition = this.GameObject;
         //PrefabPosition.position = PlaneEmpty.position; 
         //PrefabPosition.position = PlaneEmptyPosition.position;
         this.transform.parent = PlaneEmpty.transform;

         PrefabPosition.transform.position = new Vector3(1, 1, 1);

     }

     // Update is called once per frame
     void Update () {
      */
    }
}
